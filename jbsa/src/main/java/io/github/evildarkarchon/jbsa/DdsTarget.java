package io.github.evildarkarchon.jbsa;

/** DDS encode or reconstruction target, independent of archive family and destination policy. */
public enum DdsTarget {
  /** PC DDS layout. */
  PC,
  /** Xbox DDS layout. */
  XBOX
}
