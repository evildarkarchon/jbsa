package io.github.evildarkarchon.jbsa.cli;

/** Launches the thin JBSA command-line consumer. */
public final class Main {
  private Main() {}

  /**
   * Starts the compileable CLI skeleton.
   *
   * @param arguments command-line arguments reserved for the later CLI implementation
   */
  public static void main(String[] arguments) {
    // The foundation deliberately exposes no CLI behavior before the public archive contract
    // exists.
  }
}
